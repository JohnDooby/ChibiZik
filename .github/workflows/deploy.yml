name: Build and Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Ou 'master' selon ta branche principale

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies 📦
        run: npm ci

      - name: Build 🏗️
        # REMPLACE 'chibizik' par le nom de ton repo GitHub ci-dessous
        run: npx ng build --configuration production --base-href /chibizik/

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist/chibizik # Vérifie que ton build Angular va bien dans ce dossier
          branch: gh-pages # La branche où le site sera hébergé